# RAKABA — Pipeline 2 dépendances
# Versions épinglées pour reproductibilité
# (sqlalchemy retiré : Pipeline 2 utilise maintenant le DuckDB partage, voir pipeline2/database.py)

fastapi==0.111.0
uvicorn[standard]==0.29.0
duckdb>=1.0
pydantic==2.7.1

# ML
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2

# Graphe
networkx==3.3

# GNN (optionnel — commenter si PyTorch non disponible)
# torch==2.3.0
# torch-geometric==2.5.3
